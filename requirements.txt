# CPU-only JAX stack for causal-genx
# Install into a fresh conda env, then `pip install -r causal-genx/requirements.txt`

jax[cpu]
flax
optax
orbax-checkpoint
tensorboard
# NumPy 1.26 has no Python 3.13 wheels and would require a local C compiler.
# Let JAX select a compatible NumPy release (NumPy 2 on Python 3.13+).
numpy
pandas
pillow
imageio
matplotlib
fsspec
scipy
gcsfs==2024.6.1
