dataset:
  name: cxr_rait
  root: gs://cxr-rait/cxr-demography-data
  input_res: 224
  pad: 0
  augment: true

optimizer:
  lr: 0.0001
  weight_decay: 0.1
  batch_size: 32

workflow:
  type: finetune-predictor
  epochs: 100
  checkpoint_freq: 1
  execution_mode: auto
  freeze_backbone: false
  backbone_lr_scale: 0.1
  warmup_epochs: 5
  label_smoothing: 0.05
  torchxray_preprocessing: true
  dropout_rate: 0.2
  pretrained_weights_path: gs://cxr-rait/checkpoints/pretrained/torchxrayvision_densenet121_flax.npz

artifacts:
  root: checkpoints
  remote_root: gs://cxr-rait/checkpoints
  run_name: predictor_densenet121-unfrozen_jax-gpu_03-08-2026

seed: 7
runtime:
  accelerator: gpu
  gpu_id: "0"
  precision: bf16
